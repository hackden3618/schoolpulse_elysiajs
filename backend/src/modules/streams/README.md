# Streams Module

## Purpose
Manages the different streams (or tracks) within a school.

## Responsibilities
- Define stream names (e.g., North, South, Science, Arts).
- Link streams to specific schools.

## Dependencies
- `PrismaClient` (Database access)
- `Schools Module`

## Public API
- `GET /streams`: List all streams.

## Future Work
- Add endpoints for creating and managing streams.
